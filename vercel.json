{
  "build": {
    "env": {
      "HUGO_VERSION": "0.148.2",
      "HUGO_ENV": "production"
    }
  },
  "installCommand": "npm install",
  "buildCommand": "npm install autoprefixer postcss-cli && env HUGO_ENV=production RELEASE=$(grep '^tag:' ../RELEASE | awk '{print $2}') hugo",
  "outputDirectory": "public"
}